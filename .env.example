# Copy this file to .env and fill in your key.
# Required for the Image → JSON plan extractor (POST /api/extract-plan).
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the Claude model used for floor-plan extraction.
# Defaults to claude-opus-4-5.
# VITE_CLAUDE_MODEL=claude-opus-4-5
